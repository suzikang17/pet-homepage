# pet-homepage — docs

> **Note (pivot):** the project pivoted from the original web-first, email-ingestion
> product to a **native iOS health-tracking app**. The web-architecture planning notes
> were removed as obsolete — recoverable from git history if needed.

## Map

- **[`planning/`](planning/index.md)** — point-in-time business/product planning
  (vision, problem/users, pricing, competition, hardware, partnerships, risks,
  roadmap). Lore type `planning`; most are frozen Phase-0 chapters, the roadmap
  is current.
- **[`features/`](features/index.md)** — the PM-facing feature inventory: what
  exists today, status, entry points. Lore type `feature`.
- **[`20-architecture.md`](20-architecture.md)** — how the system hangs together.
- **`superpowers/specs/` + `superpowers/plans/`** — frozen designs and
  implementation plans, per feature/refactor.
- **[`devlog/`](devlog/index.md)** — day-by-day session history (lore type `devlog`).
- **`decisions/`** — ADRs.

Who should read which: see the wiki concept *who-reads-what*
(`~/dev/wiki/concepts/who-reads-what.md`).

Planning notes use `[[wikilinks]]` between each other; some may point to removed
(web-era) notes — that's expected.
